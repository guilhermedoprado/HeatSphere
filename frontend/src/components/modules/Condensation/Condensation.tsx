import styles from './Condensation.module.css';

const Condensation = () => {
  return (
    <section className={styles.container}>
      <h2 className={styles.title}>Condensation</h2>
      <p className={styles.description}>
        Film and dropwise condensation models and assumptions.
      </p>
    </section>
  );
};

export default Condensation;
