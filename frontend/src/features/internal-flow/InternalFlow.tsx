import styles from './InternalFlow.module.css';

const InternalFlow = () => {
  return (
    <section className={styles.container}>
      <h2 className={styles.title}>Internal Flow</h2>
      <p className={styles.description}>
        Models for forced convection inside pipes and ducts.
      </p>
    </section>
  );
};

export default InternalFlow;
